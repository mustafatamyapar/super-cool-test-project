# Orders API

Nothing is served over HTTP yet. This file exists so the shape of the API can be
argued about before anyone writes it.

`POST /orders` would take a table number and a list of item names, and return a
total in cents.
